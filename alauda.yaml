steps:
- name: Unit test
  container: "golang"
  groovy:
  - |+
    try {
      sh script: "make test", label: "unit tests..."
    } finally {
      junit allowEmptyResults: true, testResults: 'pkg/**/*.xml'
    }
